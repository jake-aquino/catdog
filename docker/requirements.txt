# Core
fastapi>=0.111
uvicorn[standard]>=0.30
pillow>=10.0
numpy>=1.26

# Training / evaluation
scikit-learn>=1.4

# CLI & misc
python-multipart>=0.0.9    # file uploads in FastAPI forms
jinja2>=3.1                 # if you render an HTML template in app/
tqdm>=4.66                  # optional progress bars

# Dev/test (optional but handy)
pytest>=8.2