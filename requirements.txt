# --- Core ML ---
# On macOS: use Apple Silicon build of TensorFlow
tensorflow-macos==2.16.*; sys_platform == "darwin"
tensorflow-metal==1.2.*; sys_platform == "darwin"

# On Linux (e.g., Docker): use standard TensorFlow
tensorflow==2.16.*; sys_platform != "darwin"

pillow>=10.0
numpy>=1.26

# --- Training / evaluation ---
scikit-learn>=1.4
tqdm>=4.66   # progress bars

# --- Serving / API ---
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9    # for file uploads
jinja2>=3.1                # if you render HTML templates

# --- Dev / testing ---
pytest>=8.2
